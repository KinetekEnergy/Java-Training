import java.util.*;

public class lambdas {
    public static void main(String[] args) {
        Scanner scanf = new Scanner(System.in); // scanf
        // <TYPE> input = scanf.next<TYPE>(); format
        // next.Int() for integers

        

        /*
         * Create a counter using Lambdas (Hint: Use class variables)
         * - Create a Consumer that takes in an integer
         * - Create a Supplier that returns the integer
         * - Create a Runnable that prints every positive integer out until the counter
         * reaches the number by calling the other two lambdas
         */
    }
}
